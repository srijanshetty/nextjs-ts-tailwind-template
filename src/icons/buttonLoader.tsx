import ReactLoading from 'react-loading';

const Loading = () => (
  <div className={`flex items-center justify-center`}>
    <ReactLoading type="bubbles" color="#927CFF" height="27px" width="27px" />
  </div>
);

export default Loading;
